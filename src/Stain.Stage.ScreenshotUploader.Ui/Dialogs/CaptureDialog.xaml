<UserControl x:Class="Stain.Stage.ScreenshotUploader.Ui.Dialogs.CaptureDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stain.Stage.ScreenshotUploader.Ui.Dialogs"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             Width="{Binding Width}"
        Height="{Binding Height}"
            >
    <Grid x:Name="grid">
        <Grid.Background>
            <ImageBrush ImageSource="{Binding Background}" />
        </Grid.Background>
        <Rectangle Margin="{Binding Margins}" Stroke="Red">
        </Rectangle>
    </Grid>
</UserControl>
